apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "virtual-library.fullname" . }}-test-connection"
  labels:
    {{- include "virtual-library.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "virtual-library.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
